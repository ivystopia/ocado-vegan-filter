__pycache__/
*.py[cod]
.venv/

# Local SQLite databases and backups are generated working data, not git history.
*.sqlite*

# Raw scrape streams are retained on disk for analysis but are too large/noisy for git.
*.jsonl
